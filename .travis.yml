
sudo: false
language: java
jdk:
  - oraclejdk8
addons:
   apt:
   packages:
   - oracle-java8-installer
deploy:
  provider: script
  script: "mvn cobertura:cobertura"
  
skip_cleanup: true
after_success:
  ./.travis/artifact-upload.sh

